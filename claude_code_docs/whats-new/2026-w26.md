> ## Documentation Index
> Fetch the complete documentation index at: https://code.claude.com/docs/llms.txt
> Use this file to discover all available pages before exploring further.

# 26주차 · 2026년 6월 22일–26일

> claude mcp login으로 쉘에서 MCP 서버를 인증하고, ! 접두사를 사용한 쉘 모드 명령 출력에 대한 응답을 받고, /rewind로 /clear 이전 대화를 재개.

<div className="digest-meta">
  <span>릴리스 <a href="/docs/docs/en/changelog#2-1-185">v2.1.185 → v2.1.193</a></span>
  <span>2개 기능 · 6월 22일–26일</span>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">CLI에서 MCP 서버 인증</span>
    <span className="digest-feature-pill">v2.1.186</span>
  </div>

  <p className="digest-feature-lede">새로운 `claude mcp login <name>` 및 `claude mcp logout <name>` 명령은 대화형 <code>/mcp</code> 메뉴 대신 쉘에서 구성된 MCP 서버를 인증합니다. `claude mcp login`은 서버의 OAuth 흐름을 직접 실행하고 `claude mcp logout`은 저장된 자격 증명을 삭제합니다.</p>

  <p className="digest-feature-try">세션을 열지 않고 구성된 서버에 대해 OAuth 흐름 실행:</p>

  ```bash terminal theme={null}
  claude mcp login sentry
  ```

  <a className="digest-feature-link" href="/docs/docs/en/mcp#authenticate-from-the-command-line">명령줄에서 인증</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">명령 출력에 응답하는 쉘 모드</span>
    <span className="digest-feature-pill">v2.1.186</span>
  </div>

  <p className="digest-feature-lede"><code>!</code> 접두사로 실행하는 명령은 이제 출력이 트랜스크립트에 들어오면 Claude로부터 응답을 받으므로 두 번째 프롬프트 없이 <code>! npm test</code>를 실행하고 실패 이유에 대한 설명을 들을 수 있습니다. 응답 비용은 일반 프롬프트를 보내는 것과 동일합니다. 출력이 응답 없이 컨텍스트에 추가되던 이전 동작을 유지하려면 <code>settings.json</code>에서 <code>respondToBashCommands</code>를 <code>false</code>로 설정하세요.</p>

  <p className="digest-feature-try">명령을 실행하고 해당 출력에 대한 응답 받기:</p>

  ```text Claude Code theme={null}
  > ! npm test
  ```

  <a className="digest-feature-link" href="/docs/docs/en/interactive-mode#shell-mode-with-prefix">! 접두사를 사용한 쉘 모드</a>
</div>

<div className="digest-wins">
  <p className="digest-wins-title">기타 개선 사항</p>

  <div className="digest-wins-grid">
    <div><code>/rewind</code>는 이제 <code>/clear</code>가 실행되기 이전의 대화를 재개할 수 있습니다</div>
    <div>새로운 <code>sandbox.credentials</code> 설정은 샌드박스 처리된 명령이 자격 증명 파일 및 보안 환경 변수를 읽지 못하도록 차단합니다</div>
    <div>조직에서 구성한 모델 제한이 이제 모델 선택기, `--model`, <code>/model</code>, <code>ANTHROPIC\_MODEL</code>에 적용되며, 제한된 모델을 선택하면 "restricted by your organization's settings" 메시지가 표시됩니다</div>
    <div>새로운 <code>autoMode.classifyAllShell</code> 설정은 모든 Bash 및 PowerShell 명령을 자동 모드 분류기로 라우팅하며, 거부 사유가 트랜스크립트, 거부 토스트 및 <code>/permissions</code>에 표시됩니다</div>
    <div>새로운 <code>claude\_code.assistant\_response</code> OpenTelemetry 로그 이벤트는 모델의 응답 텍스트를 전달합니다. 이미 프롬프트 콘텐츠를 로깅하는 배포 환경은 업그레이드 시 이를 수신하기 시작하므로 프롬프트만 유지하려면 <code>OTEL\_LOG\_ASSISTANT\_RESPONSES=0</code>을 설정하세요</div>
    <div>백그라운드 서브에이전트는 이제 자동 거부하는 대신 기본 세션에 권한 프롬프트를 표시합니다. 대화 상자에는 요청하는 에이전트가 표시되고, Esc는 해당 도구만 거부합니다</div>
    <div><code>/install-github-app</code>은 이제 GitHub App만 설치하고 Actions 워크플로우 및 비밀 단계를 건너뛸 수 있습니다</div>
    <div>샌드박스 네트워크 권한 대화 상자에서 허용한 호스트는 연결할 때마다 다시 프롬프트를 표시하는 대신 세션의 남은 시간 동안 기억됩니다</div>
    <div>스트리밍 응답의 CPU 사용량이 약 37% 감소하고 터미널 출력 캐시로 인한 대규모 세션 메모리 증가가 감소했습니다</div>
    <div>`/review <pr>`은 이제 <code>/code-review medium</code>과 동일한 리뷰 엔진을 사용합니다</div>
    <div>Bash 모드의 <code>!</code> 명령에서 실시간 파일 경로 자동 완성을 제공합니다</div>
  </div>
</div>

[v2.1.185–v2.1.193 전체 변경 로그 →](/docs/en/changelog#2-1-185)
